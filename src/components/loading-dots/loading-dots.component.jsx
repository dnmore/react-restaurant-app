import { Loader, LoaderElement } from "./loading.dots.styles";

const LoadingDots = () => {
  return (
    <Loader>
      <LoaderElement />
      <LoaderElement />
      <LoaderElement />
    </Loader>
  );
};

export default LoadingDots;
